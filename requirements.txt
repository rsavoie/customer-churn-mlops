# Versiones fijadas (pinned) para builds reproducibles: un rango abierto deja que cada build
# resuelva versiones distintas, y en la nube eso puede romper el arranque de la imagen.
# El stack numerico (numpy/scipy/scikit-learn) se fija junto para evitar incompatibilidades de ABI.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pandas==2.3.3
scikit-learn==1.9.0
joblib==1.6.0
numpy==2.5.2
scipy==1.18.1
